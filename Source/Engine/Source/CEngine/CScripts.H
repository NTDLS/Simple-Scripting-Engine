#ifndef _CScript_H_
#define _CScript_H_
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#include "CException.H"

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

typedef struct _tag_CScript_Item {
	char *FileName;
	char *Namespace;
} SCRIPTITEM, *LPSCRIPTITEM;

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

typedef struct _tag_CScript_Collection {
	int Count;
	SCRIPTITEM *Items;
} SCRIPTCOLLECTION, *LPSCRIPTCOLLECTION;

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class CScripts {

public:
	CScripts(void *lpEngine);
	~CScripts();
	CException *Error;
	SCRIPTCOLLECTION Collection;

	SCRIPTITEM *GetNameSpace(const char *sName);
	SCRIPTITEM *GetPartialNameSpace(const char *sName);
	SCRIPTITEM *GetPartialNameSpace(const char *sName, bool *fullQualified);
	ErrorLevel AddFile(const char* sFileName);
	ErrorLevel AddString(const char* sScriptText);
	bool IsDefined(int iIndex);
	SCRIPTITEM *GetPointer(int iIndex);

private:
	void *pEngine; //Pointer to the Engine Class.
	int PopulateLines(SCRIPTITEM *pItem);
};

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#endif

